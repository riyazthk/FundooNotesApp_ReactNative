import {StyleSheet} from 'react-native';
const EmailStyles = StyleSheet.create({
  userEmailHeader: {
    height: '100%',
    justifyContent: 'space-around',
  },
  userEmailView: {
    height: 40,
  },
  UserEmailinnerCardText: {
    textAlign: 'center',
    fontSize: 25,
  },
});
export default EmailStyles;
